<Page x:Class="Test.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Test"
      xmlns:uwph="using:UWPHelper"
      xmlns:triggers="using:UWPHelper.Triggers"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      >
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <triggers:IsTypePresentTrigger Type="Windows.UI.ViewManagement.StatusBar"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PhoneButt.Visibility" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <ContentPresenter Content="TEST"
                          Style="{StaticResource PageTitle}"
                          Grid.Row="0"
                          />

        <StackPanel Padding="25" Grid.Row="1">
            <CheckBox x:Name="WatBox"
                      Content="Wat"
                      />
            <CheckBox x:Name="WutBox"
                      Content="Wut"
                      IsChecked="{x:Bind WatBox.IsChecked, Mode=TwoWay, Converter={StaticResource InvertedBoolConverter}}"
                      />

            <ComboBox SelectedIndex="{x:Bind WatBox.IsChecked, Mode=TwoWay, Converter={StaticResource BoolToIntConverter}}">
                <ComboBoxItem Content="0"/>
                <ComboBoxItem Content="1"/>
            </ComboBox>

            <ComboBox SelectedIndex="{x:Bind TestEnum, Mode=TwoWay, Converter={StaticResource EnumToIntConverter}}">
                <ComboBoxItem Content="0"/>
                <ComboBoxItem Content="1"/>
                <ComboBoxItem Content="2"/>
                <ComboBoxItem Content="3"/>
            </ComboBox>

            <Rectangle Fill="Red"
                       Width="50"
                       Height="50"
                       HorizontalAlignment="Left"
                       Visibility="{x:Bind WatBox.IsChecked, Mode=TwoWay, Converter={StaticResource BoolToVisibilityConverter}}"
                       />

            <Button x:Name="PhoneButt" Content="Mobile" Visibility="Collapsed"/>

            <uwph:AboutApp/>
        </StackPanel>
    </Grid>
</Page>
